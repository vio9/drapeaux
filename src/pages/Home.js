import React from "react";
import Navigation from "../components/Navigation";
import Logo from "../components/Logo";

const Home = () => {
	return (
		<div>
			<Navigation />
			<Logo />
			<h1>Accueil</h1>
		</div>
	);
};

export default Home;
